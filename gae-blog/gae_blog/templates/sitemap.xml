<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {% for blogPost in blogPosts %}	
    <url>
      <loc>{{baseURL}}/{{blogPost.slug}}/</loc> 
      <lastmod>{{blogPost.publishDate}}</lastmod>
    </url>
  {% endfor %}
</urlset>